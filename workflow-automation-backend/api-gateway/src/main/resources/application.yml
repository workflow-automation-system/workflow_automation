server:
  port: 8085

jwt:
  secret: ${JWT_SECRET:mySecretKey123456789012345678901234567890}

spring:
  application:
    name: api_gateway
  cloud:
    gateway:
      # This cleans up any duplicate headers if they occur
      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Origin Access-Control-Allow-Credentials, RETAIN_FIRST
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true

      routes:
        - id: auth-service
          uri: ${AUTH_SERVICE_URI:http://localhost:8081}
          predicates:
            - Path=/api/auth/**
        - id: workflow-service
          uri: ${WORKFLOW_SERVICE_URI:http://localhost:8082}
          predicates:
            - Path=/api/workflows/**, /api/executions/**, /api/integrations/**
        - id: organization-service
          uri: ${ORGANIZATION_SERVICE_URI:http://localhost:8083}
          predicates:
            - Path=/api/organizations/**
        - id: template-service
          uri: ${TEMPLATE_SERVICE_URI:http://localhost:8084}
          predicates:
            - Path=/api/templates/**